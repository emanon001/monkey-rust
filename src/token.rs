#[derive(Debug, PartialEq, Eq)]
pub enum Token {
    Assign,
    Asterisk,
    Bang,
    Comma,
    EOF,
    Else,
    False,
    Function,
    GT,
    Ident(String),
    If,
    Illegal(String),
    Int(String),
    LBrace,
    LParen,
    LT,
    Let,
    Minus,
    Plus,
    RBrace,
    RParen,
    Return,
    Semicolon,
    Slash,
    True,
}
